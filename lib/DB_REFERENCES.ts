// * Reference names of db variables from this file so in case of any name changes, it only needs to be changed in one place

export const dbNames = {
    ledger: "ledger",
};

export const dbCollections = {
    manage: "manage",
    transactions: "transactions",
};
